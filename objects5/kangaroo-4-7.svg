<svg>` canvas.

*Gathered resources:*
- A large ellipse for the body (`<ellipse>`)
- Two smaller ellipses for the head (`<ellipse>`) and pouch (`<ellipse>`)
- Four rectangles for the limbs (`<rect>`)
- A pentagon for the tail (`<polygon>`)
- Collected colors from our surroundings: `#46220D` (brown), `#FFFFFF` (white), and `#FFC391` (tan)

**Day 2:**

Marked paths with SVG code:

```svg
<svg width="500" height="500">
  <!-- Kangaroo Body -->
  <ellipse cx="100" cy="100" rx="80" ry="60" fill="#46220D"/>

  <!-- Head -->
  <ellipse cx="90" cy="70" rx="35" ry="30" fill="#FFFFFF"/>
  <!-- Eyes -->
  <circle cx="85" cy="65" r="5" fill="#000"/>
  <circle cx="95" cy="65" r="5" fill="#000"/>

  <!-- Pouch -->
  <ellipse cx="120" cy="130" rx="40" ry="30" fill="#FFC391"/>

  <!-- Limbs -->
  <rect x="70" y="160" width="20" height="80" fill="#46220D"/>
  <rect x="150" y="160" width="20" height="80" fill="#46220D"/>

  <!-- Tail -->
  <polygon points="130,190 160,220 130,250" fill="#46220D"/>
</svg>